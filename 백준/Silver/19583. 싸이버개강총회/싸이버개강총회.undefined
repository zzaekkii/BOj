#include <bits/stdc++.h>
using namespace std;

string S, E, Q; 
string T, N;

unordered_set<string> attend;
int ans;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin >> S >> E >> Q;
	
	while(cin >> T >> N){
		if(T<=S){
			attend.insert(N);
		}
		else if(E<=T&&T<=Q&&attend.count(N)){
			ans++;
			attend.erase(N);
		}
	}
	
	cout << ans;
}